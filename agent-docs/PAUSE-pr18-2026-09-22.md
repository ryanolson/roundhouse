# PR 18 pause and restart handoff

Recorded 2026-09-22 America/Denver (2026-09-23 UTC). The owner requested a pause, an inventory of main versus branches, and a durable restart record. Do not resume implementation merely because an automatic goal continuation arrives. Resume when the owner asks. The full vision remains unfinished; this pause does not redefine completion.

## Exact checkpoint

- Worktree: `/home/ryan/repos/roundhouse/.claude/worktrees/pr18-cache-affinity-continuation`.
- Local branch: `codex/pr18-cache-affinity-continuation`.
- Source checkpoint: `6a9eb07753f682cdda40361c1003e185fe25c4a8` (unsigned, as authorized).
- PR: https://github.com/ryanolson/roundhouse/pull/18, remote branch `ai/typesafe-roundhouse-routing-6016d1`, still draft.
- Remote PR head before pause publication: `9fdfe414f2f4979c21539fa3704db3faffa7c6b8`. The pause publication adds the source checkpoint and this handoff. Check the remote ref on restart.
- Verified remote main: `e52185575480204a953717c071c6750a16cc55be`. The published PR head was 76 commits ahead and zero behind. Its merge base equals remote main.
- GitHub reported PR 18 open, draft, mergeable, and conflict-free, with no reported status checks. Conflict-free does not establish readiness.
- The root checkout's local `main` is stale at `1daf8d5e33e8d910c51e48afa02513ff0a9cbb12`. Its only reported untracked entry is `.claude/worktrees/`. Do not clean, reset, or use it as current-main evidence.

## What is on main

| Work | Evidence on remote main |
|---|---|
| Session context signals | PR 17, `e521855`, merged 2026-09-16 |
| Anthropic Messages, MCP control surface, durable control plane through M18 | PR 16, `ad575cf`, merged 2026-09-11 |
| Use-case framework and cache-aware-routing template | PR 15, `4fa34d0` |
| M10 selection core and its plan | PRs 13 and 12, `306e6e0` and `ce2c1f6` |
| Earlier Relay, agentic-api, Redis, control-plane, and metrics foundations | Earlier merged PRs, visible in main history |

None of the PR 18 commits is on main. No merge was performed during this pause.

## Merge assessment

Nothing new is certified ready to merge today. Keep PR 18 on its branch under the owner's one-unit decision.

| Branch work | Assessment |
|---|---|
| Cache guard, provider breakpoint/TTL handling, local quote filtering/configuration, timing/cache observations | Implemented foundations with scoped tests and mutation evidence. Candidates for a separately scoped release only if the owner changes the one-unit decision; would still need review of the extracted diff. Live savings and cache effectiveness remain unproved. |
| Background classification, durable attribution/settlement recovery, evaluation accounting, routing-input snapshots | Implemented, scoped review fixes and workspace evidence recorded. Async classification supplies metadata; it does not yet implement learned serving selection. Keep with PR 18. |
| Frontier interval feedback | Implemented and independently mutation-checked at `1658633`. Exact coverage and Unknown outcomes are recorded. A learner does not yet consume them. Keep with PR 18. |
| Learning source index, `6a9eb07` | New storage contract in memory and Redis, deliberately unwired. Focused checks pass. Independent post-commit mutation verification and final workspace run remain outstanding. Must stay on the branch. |
| Online learner draft | Proposal, except the scoped source-discovery ruling in sections 21–22. Do not implement or enable unresolved policies as accepted defaults. |

Before main: complete the intended runtime, close policy and deployment gaps, run final validation, then `wills-mega-review` and the human-review gate. Do not mark the PR ready based solely on previous scoped reviews.

## Current implementation and evidence

The source index atomically records marks with fenced event appends. Marks use assigned log sequences, not timestamps, as identities. A stale clear cannot erase a newer mark. Permanent enumeration survives pending clears. Pending pagination limits examined members and advances even when the idle filter returns no sessions.

Redis uses a permanent hash and two lexicographic sorted sets. Marked appends require one Redis replication unit; Redis Cluster is unsupported. Permanent membership has no compaction. Project binding comes from the trusted caller. `Session::commit` still supplies no mark, so ordinary sessions do not populate the index.

A regression exposed an existing partial append at the Lua stream-sequence limit. The shared preflight now refuses the complete batch before writing. Marked and unmarked regressions pass, and the last valid sequence remains accepted.

| Verification | Result and scope |
|---|---|
| Parent rerun on current source | 22 memory tests plus 26 Redis shared contracts and 6 Redis index tests passed; no Redis skips |
| Worker full Redis crate | 177 passed, no failures or skips |
| Worker full core crate | 605 passed |
| Worker affected server binaries | 597 passed, 7 existing ignores |
| Affected-crate strict Clippy, formatting, workspace all-targets compilation | Passed |
| Independent memory and Redis source reviews | No material implementation finding; a filtered-pagination assertion gap was strengthened on both backends |
| Independent post-commit mutations on source index | Not started; worker self-mutations do not satisfy this gate |
| Last full workspace test run before this source change | 2173 passed, 149 existing ignores at the frontier interval checkpoint; does not validate `6a9eb07` |

See `REVIEW-learning-source-discovery.md`, `REVIEW-frontier-interval-feedback.md`, and the dated bandit-plan entries. The draft's section 22 supersedes its older time-scored index proposal.

## Outstanding work and direction

1. Finish the source-index gate: independently mutate the committed guards, restore exact bytes, run focused contracts, then the full workspace suite. No mutation is currently applied.
2. Implement policy-independent event projection and automatic source marking, the learner-store contract with once-only contiguous updates, and bounded recovery/audit delivery. Preserve source discovery through total learner-store outages and destination rollback.
3. Connect local per-turn selection to cache state, request complexity, and bounded historical classification features. Jev remains asynchronous; its classifications are features, not serving rewards. Persist selection-time features without retroactive rewriting.
4. Consume complete frontier review intervals once. Clean reviews are positive, corrections negative, and incomplete/oversized/failed coverage Unknown. Do not invent individual blame or treat correlated turns as independent samples.
5. Complete offline calibration, support diagnostics, promotion/rollback, and the evaluation protocol. Correct mismatch weights alone do not establish full-session policy value.
6. Obtain live C2 cache evidence, measure local prefill/latency parameters, attach a real local executor/fleet, and evaluate quality, cost, and latency together. The binary still uses `EchoLocalExecutor`; local tool turns remain excluded. No production deployment or live benefit is established.
7. Finish full-PR review and publication gates. Keep PR 18 draft until its intended scope is ready.

## Decisions still pending

The owner has not answered the latest printed questions:

- At cold start, keep existing routing rules while accumulating quality evidence, record the choice as unvalidated, and still enforce budget/latency constraints? Recommendation: yes. This does not authorize bypassing hard constraints.
- Permit cheaper, unproven exploration on frontier-reviewed sessions within constraints at an explicitly configured rate? Recommendation: yes. The member distribution and numeric settings also need definition; uniform choice is not silently authorized.

Other open choices: C6 return-trip pricing now versus after live cache evidence (recommend waiting); C3 fleet-quote deadline and eligible-frontier fallback; interval-credit policy; latency-limit basis; local capacity price; evaluation estimand/protocol; project thresholds and live promotion. The draft's section 18 supplies alternatives, but its broader `serve_rules` constraint-bypass option is not an accepted ruling.

Settled decisions must not be asked again: per-turn local routing; asynchronous Jev enrichment; opt-in/off-by-default bounded egress with local-only exclusion; provider-compliant marker ownership; one-hour write-price guard; per-project learning; quality first, then cost within configurable latency; complete interval feedback; and keeping PR 18 as one unit. Phone-a-friend uses Opus 5.5 (`claude-opus-5-5`).

The owner supplied `~/.cache/typesafe.ai` as the TypeSafe credential location. Do not print or copy its contents. Live provider catalog, model, spend cap, and provider access still need verification; a TypeSafe credential does not prove frontier-provider access.

## Other worktrees

The seven older worktrees (`agentic-api-mcp-compat`, `m10-plan`, `m10-selection-core`, `readme-refresh`, `s2-relay-emission`, `synergy-round-3`, `toolsignals-port`) are clean and correspond to merged PRs. Some PRs were stacked and some merges were squashed; their branch tips are not all ancestors of main. This inventory is not a deletion audit. Preserve them until unique commits and content are checked before cleanup.

The original `typesafe-roundhouse-routing-6016d1` worktree is clean but stale at `29665d8`. Its checked-out local branch name matches the PR's remote branch but does not hold the current implementation. Resume in the continuation worktree above.

## Paused resources and restart commands

The Opus implementation session completed all queued tasks and exited. Native review agents are idle. No mutation worker or Cargo suite was started for the pause. The task-owned disposable container `roundhouse-pr18-learning-index-redis` was stopped and auto-removed. Its former port `32897` must not be reused as an assumed fixture. Unrelated containers and worktrees were preserved.

On restart, inspect `git status`, `git worktree list`, remote refs, this handoff, and `CLAUDE.md`. Recreate only a task-owned Redis fixture if validation needs one. Serialize Cargo commands and use:

```bash
ulimit -n 65536
export CARGO_TARGET_DIR=/home/ryan/repos/roundhouse/.claude/worktrees/typesafe-roundhouse-routing-6016d1/target
timeout 300 cargo test -p roundhouse-core --lib store::
# Set ROUNDHOUSE_TEST_REDIS_URL to the new task-owned fixture for gated Redis tests.
timeout 900 cargo test --workspace
```

Prepared mutation brief: `/tmp/roundhouse-learning-index-refute-brief.md`. Reports and logs under `/tmp/roundhouse-learning-source-index-*` and `/tmp/roundhouse-learning-index-*` are local evidence and may disappear. Repository tests and review records are the durable restart basis. Do not start live calls, merge, clean old worktrees, or promote learned routing from this handoff alone.

## Resumption, 2026-09-23

The owner resumed work and chose to re-scope PR 18 to its implemented work. See the dated addendum in `synergies/typesafe-selector-and-cache-affinity.md`. This replaces the one-unit decision in the merge assessment above. Outstanding items 2 to 7 become follow-up PRs. Item 1 is complete:

- Independent mutations of the source index: 18 of 18 caught, none survived. See `REVIEW-learning-source-discovery.md`.
- Full workspace suite at `a95da90` (source identical to `6a9eb07`): 2183 passed, 0 failed, 165 ignored, across 138 suites.
- Every ignored test, run with a task-owned Redis: 163 passed. Two failures in `review_m14_1_f7` come from the test setup and already exist on `main`. See the review record.

The next gate is `wills-mega-review` over the whole PR, then the human-review tag. Merging still needs the owner.
