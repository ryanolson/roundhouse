# TODO: Corpus Title

> TODO: Replace this file with the shared, stable context for your use case. This is sent
> verbatim as the system prompt (`instructions`) on every turn of every session, by every user.
> That is the point: it is the long, identical prefix whose reprocessing cost the demo measures.
>
> Guidelines:
> - Make it large enough to exceed `min_prefix_tokens` (default 1024) in your catalog.json.
>   At ~4 chars/token, that is roughly 4 KB of text. 2–10 KB is a good target for a demo.
> - Make it self-contained and fictional (no live external data dependency).
> - Design the questions in turns.jsonl to be answerable *only* from this document,
>   with a clear "not specified" contract for anything outside it.

TODO: Replace this with your domain document.
